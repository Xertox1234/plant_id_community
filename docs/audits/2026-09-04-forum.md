# Audit: Community Forum — Post-Sprint Full Audit

> **Date:** 2026-09-04
> **Trigger:** User-invoked `/audit` after a run of forum-only PRs (#589 polls, #606/#607 RAG, #619/#620 spam trust gate + timeout, #623 Green Thumb alignment, #624/#625 Wagtail quick wins, #626 topic-path redirects, #627 polish). Phases 1–2.5 ran read-only first (findings report); the user then chose **fix every Medium and Low, file H1 as a todo** — Phases 3–8 ran on the same branch.
> **Domains:** security, performance, django-drf, wagtail, react-typescript, flutter, celery, testing
> **Baseline:** backend forum suite (`packages/wagtail_forum` + `apps/forum_host`) 1189 passed / 0 failed, `manage.py check` clean | web 1054 passed (89 files), `tsc --noEmit` clean, eslint 0 errors (1 warning in the `coverage/` artifact) | mobile `flutter analyze` clean, forum tests 201 passed
> **Worktree:** `.worktrees/audit-2026-09-04` on branch `audit/forum-2026-09-04` (HEAD = `56ad617`)
> **Agents dispatched (Phase 2, read-only):** django-drf-reviewer, wagtail-reviewer, cross-cutting-reviewer ×2 (security/API; performance/testing), react-typescript-reviewer, flutter-dart-reviewer, celery-async-reviewer. Residue sweep after every agent exited: worktree clean except this manifest; no `MUTANT`/probe markers.
> **Phase 2.5:** 6 docs-researcher agents (Context7): 19 confirmed, 1 better-fix, 2 not-applicable, 0 contradicted.
> **Phase 3 review gate:** `kimi-review` is not installed on this machine (the commit hook silently no-ops without it), so the per-fix gate was: targeted tests + a mutation check on every new pin (M1, M2, L12) + the Phase 6 domain reviewers over the staged diff.
> **Phase 6:** 6 domain reviewers over the staged diff (django-drf, wagtail, celery, react, flutter, cross-cutting): 0 blocking; 1 HIGH (L8 `_pending` back-guard) + 5 MEDIUM fixed in-branch; 3 LOW/MEDIUM became todos 336 (Playwright mention spec), 337 (`update_index` backfill), 338 (`Topic.author blank=True`); the app_router_test.dart reflow is `dart format` output the pre-commit hook requires.
> **Close baseline:** backend forum + users suites 1336 passed, `manage.py check` clean, `makemigrations --check` clean | web 1068 passed (89 files), `tsc --noEmit` clean, eslint clean | mobile `flutter analyze` clean, 427 passed, `build_runner` regenerated `app_router.g.dart`.
> **Live checks:** Railway project `PlantID Community` has exactly four services (web `plant_id_community`, `Postgres`, `Redis`, `forum-prune-cron`) — no Celery worker. Redis memory flat at ~12 MB over the last 7 days.

## Findings

Each finding has a lifecycle: `open` → `fixing` → `verified` or `deferred` or `false-positive`.

**Status key:**

- `open` — Found but not yet addressed
- `fixing` — Work in progress
- `verified` — Fix applied AND confirmed by test/grep/type-check
- `deferred` — Intentionally postponed (must link to todo)
- `false-positive` — Agent was wrong or issue was already fixed

**Research key** (Phase 2.5 verdict, recorded in the `Research` column):

- `confirmed` — current documentation agrees the finding is valid
- `better-fix` — finding is real, but current docs show a cleaner fix (described in the `Verification` column for Phase 3 to use)
- `contradicted ⚠` — current docs say the flagged pattern is fine; may be a false positive — decide at triage
- `—` — research not applicable, or finding predates Phase 2.5

**Severity note:** where the auditor's severity differs from the reporting agent's, the agent's rating and the reason for the change are recorded in the Verification column. Every finding below was re-read in current code by the auditor before being recorded.

### Critical

| ID  | Finding | Domain | Agent | File(s) | Research | Status | Verification |
| --- | ------- | ------ | ----- | ------- | -------- | ------ | ------------ |
| —   | none    | —      | —     | —       | —        | —      | —            |

### High

| ID  | Finding | Domain | Agent | File(s) | Research | Status | Verification |
| --- | ------- | ------ | ----- | ------- | -------- | ------ | ------------ |
| H1  | No Celery worker is provisioned anywhere (tracked config **and** live Railway topology), yet five forum tasks are enqueued with `.delay()` on shipped request paths: `send_forum_push`, `send_forum_push_batch`, `send_forum_email_batch`, `sync_blog_page_chunks`, `generate_topic_summary`. Reply/mention/moderation pushes and reply emails never send; a premium `GET topics/<id>/summary/` returns `202 pending` forever (the `cache.add` lock only dedups enqueues); enqueued messages sit in the Redis broker list with no TTL. This is the todo-261 deferral ("defer the worker until push/email/summaries are turned on") whose trigger has partly arrived — `OPENAI_API_KEY` is live in prod for the spam backend — but no follow-up ever added the worker. | celery | celery-async-reviewer | `backend/railway.json:8`, `backend/Dockerfile:66`, `backend/railway.cron.json`, `backend/docs/deployment/railway.md:110-133,233-240`, `backend/apps/forum_host/tasks.py`, `notifications.py:119,144,160,278,320`, `summary.py:116`, `signals.py:74`, `settings.py:1101` | confirmed — Celery docs: no message expiry unless `expires`/`task_default_expires` is set; `visibility_timeout` only matters once a worker has fetched; nothing is written to the result backend at enqueue time. Correct rule doc is `railway.md`, not `services.md`. | deferred | Agent rated **Critical**; recorded as **High**: in-app bell notifications are created synchronously (unaffected); no web or mobile client calls the summary endpoint (grep clean); push is gated on `FIREBASE_CREDENTIALS_PATH` and email on `EMAIL_HOST`, whose prod values were not inspected; Railway Redis memory is flat (~12 MB / 7 d) so the backlog is not a visible cost today. Needs an ops decision: add the worker (co-located per the runbook) or gate the summary endpoint and stop enqueuing until it exists. **Deferred by user decision** (deploy-topology + spend call, not a code change) → `todos/335-pending-p2-forum-celery-worker.md`. The two code-side prerequisites (M8 backoff factor, L3 `ignore_result`) shipped here. |

### Medium

| ID  | Finding | Domain | Agent | File(s) | Research | Status | Verification |
| --- | ------- | ------ | ----- | ------- | -------- | ------ | ------------ |
| M1  | Topic-path redirects miss the flow the module's own comment promises: live topic → unpublish → moderator edits `slug`/`board` in `/cms/` → Publish. For a non-live `DraftStateMixin` snippet the edit view saves the form with `commit=True`, so the new slug is on the DB row before publish; `publish_revision` then does an unrestricted `object.save()`, the `pre_save` snapshot reads the already-changed row, old == new, and no `Redirect` is written. The once-public URL 404s. Existing test `test_an_unpublished_topic_changing_slug_creates_nothing` starts unpublished so never had a public path; no test drives the admin edit view. | wagtail | wagtail-reviewer | `backend/apps/forum_host/redirects.py:62-89` (comment 80-82), `wagtail_forum/models/topics.py:119,121` (panels), installed `wagtail/admin/views/generic/mixins.py:518-521`, `wagtail/actions/publish_revision.py:168` | **better-fix** — Wagtail 6.0 release notes document that non-live draft edits now write through to the DB row; `wagtail.contrib.redirects` auto-redirects are Page-only (`page_slug_changed`, fired on every save via `on_commit`). Cleaner fix: snapshot the old path from `DraftStateMixin.live_revision` (only reassigned by an actual publish) — resolve `Revision.as_object().get_absolute_url()` when `live_revision_id` changes — instead of diffing the raw row. | verified | Agent rated **High**; recorded as **Medium**: the flow needs an admin slug/board edit on an unpublished topic, and the outcome is a 404 on a URL that was already 404 while hidden; but the documented guarantee is false and untested. Verified against installed Wagtail 7.4.2 source. FIXED — `redirects.py`: pre_save stashes the old path only for a once-public row (`old.live or old.first_published_at`), post_save no longer gated on the new live state, and `redirect_board_topics` includes once-public hidden topics. The researcher's `live_revision` snapshot could NOT work: `UnpublishAction` nulls it (installed `wagtail/actions/unpublish.py:59`); `first_published_at` survives unpublish and every real topic gets it via `revision.publish()` (`workflow.py`). 3 new tests (unpublish→rename→publish keeps the row; old path 301s after republish; board rename writes a row for a once-public hidden topic). Mutation-checked: restoring the live gate fails 2/3. |
| M2  | `handleReply` is the only write handler on the thread page without the stale-thread guard: after `createPost` → `revalidateIdentity` → `collectAllPosts` (up to 50 sequential fetches) it calls `setPosts`/`setNextCursor`/`setTotalPosts`/`setJustPostedId` unconditionally. Navigating thread A → thread B mid-submit reuses the same component instance, so thread A's posts land in thread B's view. Siblings at 257-281, 457-483, 491-516, 530-550 all capture `requestTopicId`. No test covers the reply path (subscribe/bookmark have one). | react-typescript | react-typescript-reviewer | `web/src/pages/forum/ThreadDetailPage.tsx:365-428`, `web/src/App.tsx:70` | confirmed — react-router-dom 7 preserves the component instance when only params change (`RenderedRoute`, no key). | verified | Agent rated **High**; recorded as **Medium**: requires navigation inside the submit window, though `collectAllPosts` makes that window seconds long on big threads. Fix is mechanical: mirror the sibling guard. FIXED — `requestTopicId` captured; every setState after an await gated on `currentTopicIdRef`. New test: a reply resolving after navigating to thread B leaves B's posts and composer untouched and announces nothing. Mutation-checked: removing the guard fails it. |
| M3  | "Your season" stats effect depends on `[isAuthenticated]` (`!!user`), not `user?.id`, and gates on `isAuthenticated` alone rather than `!isLoading && isAuthenticated`. `revalidateIdentity()` on tab focus can swap the `user` object without flipping the boolean, so account A's private stats stay on screen under account B; and a sessionStorage-seeded expired session fires a `fetchMyStats()` that 401s. `docs/rules/react.md:149` already names this file as having the unported shape (fixed on HomePage in todo 315). | react-typescript | react-typescript-reviewer | `web/src/pages/forum/CategoryListPage.tsx:50,131-152`, `docs/rules/react.md:140-156` | confirmed — React docs: `key={userId}` / identity in deps is the documented reset-per-identity mechanism. | verified | Agent rated **High**; recorded as **Medium**: needs a same-browser account switch and the leaked numbers are low-sensitivity counts; but it is a known, documented, unfixed gap. FIXED — effect keyed on `[isAuthenticated, authLoading, userId]`, gated on `!authLoading`, clears stale stats before the refetch. 2 new tests (identity swap refetches and replaces the numbers; loading gate skips the fetch). |
| M4  | Three write/search error banners are conditionally mounted with no `aria-live`/`role`, so screen readers never hear the failure; `NewThreadPage` already imports `useAnnounce()` and uses it for the success path only. `ThreadDetailPage.tsx:880-893` shows the correct always-mounted pattern (audit M26). | react-typescript | react-typescript-reviewer | `web/src/pages/forum/NewThreadPage.tsx:600-604`, `SearchPage.tsx:338-342`, `UserProfilePage.tsx:188-190` | confirmed — MDN ARIA live regions: the region must exist before its content changes; only `role="alert"` is (mostly) announced on insertion, and these carry neither. | verified | Verified all three snippets in current code. Fix: `announce(error, 'assertive')` in each catch, or an always-mounted `aria-live` container. FIXED — `announce(message, 'assertive')` on NewThreadPage submit failure, SearchPage search + load-more failures, UserProfilePage block failure. 3 new tests (real `AnnouncerProvider` region on NewThreadPage; hoisted `useAnnounce` mock on the other two). |
| M5  | Search "Load more" discards the returned `Future`; `ForumSearch.loadMore()` restores `state` to `data` in its catch and then `rethrow`s, while `search()` swallows. Result: a failed page-2 fetch is an unhandled async error (no `PlatformDispatcher.onError` handler in `main.dart`) and the button appears to do nothing. The three sibling screens `await` inside `try/catch` + SnackBar. | flutter | flutter-dart-reviewer | `plant_community_mobile/lib/features/forum/screens/forum_search_screen.dart:183-184`, `providers/forum_providers.dart:486-541` | confirmed — Flutter error-handling docs (async callback errors route to `PlatformDispatcher`, not `FlutterError.onError`); Riverpod docs (`ref.listen` / awaited notifier call for side effects). | verified | Agent rated **High**; recorded as **Medium**: no crash, silent no-op + logged error. Fix: wrap like `forum_topics_screen.dart:142-151`. FIXED — awaited inside try/catch + SnackBar, same shape as the three sibling screens. New widget test: a failing page-2 search shows the SnackBar and keeps page 1. |
| M6  | `/forum/notifications` is not in `protectedRoutes` (`{profile, garden}`) and the screen never reads `authServiceProvider`, so an anonymous user gets "Could not load notifications" + a Retry that fails forever; the composer screen gates correctly. Providers are autoDispose, so no cross-account leak — the gap is the dead-end UX and no redirect on mid-session sign-out while the screen is foregrounded. | flutter | flutter-dart-reviewer | `plant_community_mobile/lib/core/routing/app_router.dart:63,240`, `screens/forum_notifications_screen.dart`, `screens/forum_composer_screen.dart:296-317` | confirmed — go_router docs: top-level `redirect` + `refreshListenable` (already implemented here) is the documented guard; this is an incomplete set, not a wrong pattern. | verified | Verified. Fix: add the route to `protectedRoutes` or mirror the composer's `isAuthenticated` check. FIXED — `AppRoutes.forumNotifications` added to `protectedRoutes` and used as the GoRoute path; new router test (anonymous `go()` lands on `/login`); `app_router.g.dart` regenerated. |
| M7  | Composer image picker passes no `maxWidth`/`maxHeight`/`imageQuality`, and neither the controller nor the API client checks size before upload, so a full-resolution phone photo (often >10 MB) is uploaded in full only to be rejected by the backend's 10 MB cap. `camera_screen.dart:44-48,64-68` already uses `1080/1080/85` for plant ID. | flutter | flutter-dart-reviewer | `plant_community_mobile/lib/features/forum/services/forum_image_picker.dart:19-21`, `forum_composer_controller.dart:101-104`, `forum_api.dart:317-338`, `backend/packages/wagtail_forum/wagtail_forum/conf.py:22` | confirmed — image_picker 1.2.2 docs: `maxWidth`/`maxHeight`/`imageQuality` honored on Android and iOS (HEIC-on-simulator caveat only). | verified | Verified; the rejection message is surfaced cleanly so this is wasted bandwidth/time, not data loss. FIXED — `maxWidth`/`maxHeight` 1080, `imageQuality` 85 (parity with `camera_screen.dart`). No unit test: platform channel behind the injected interface. |
| M8  | `send_forum_push_batch` / `send_forum_email_batch` use `retry_backoff=True` with `autoretry_for=(OperationalError,)`, i.e. backoff factor 1 (~1 s/2 s/4 s jittered) — near-instant retries against a DB that is already struggling. The sibling `sync_blog_page_chunks` (line 369) was fixed in PR #606 to pass `retry_backoff=constants.RAG_INDEX_RETRY_DELAY`; these two never got the same treatment (`docs/rules/celery.md:44-48`). | celery | celery-async-reviewer | `backend/apps/forum_host/tasks.py:197,279` (vs `:369`) | confirmed — Celery 5.6 task docs: "If True, starts at 1s; if a number, acts as a delay factor"; `default_retry_delay` is not consulted on the `autoretry_for` path. | verified | Verified both decorators. Fix: numeric factor + pin the countdown in a test as done for the sibling. Moot until H1 is resolved, but should ship with it. FIXED — `retry_backoff=constants.NOTIFICATION_BATCH_RETRY_DELAY` (30) + `NOTIFICATION_BATCH_MAX_RETRIES` on both batch tasks; config pin + countdown pin (30/60/120, `push_request` + mocked `retry()` + jitter at max). |

### Low

| ID  | Finding | Domain | Agent | File(s) | Research | Status | Verification |
| --- | ------- | ------ | ----- | ------- | -------- | ------ | ------------ |
| L1  | `ForumProfile.for_user()` re-wraps `get_or_create()` in `except IntegrityError: .get()`. Django already retries its own `.get()` inside a savepoint and only re-raises when that retry finds nothing, so the wrapper fires only in the unrecoverable case and converts a real `IntegrityError` (e.g. FK violation) into a misleading `DoesNotExist`. Hot path (every DM send and every post/topic trust check). Same anti-pattern already fixed in `topic_reads.py::mark_read`. | django-drf | django-drf-reviewer | `backend/packages/wagtail_forum/wagtail_forum/models/profiles.py:144-149` | confirmed — Django 6.0 `get_or_create` docs + source (`transaction.atomic` around `create()`, internal `IntegrityError` → `.get()` retry). `defaults` callable is evaluated lazily, as the code assumes. | verified | Agent rated **Medium**; recorded as **Low**: only the exception type changes, in a path that is already failing. Rule: `services.md:813`. FIXED — wrapper removed, unused `IntegrityError` import dropped; test rewritten to assert the unrecoverable IntegrityError propagates instead of masquerading as DoesNotExist. |
| L2  | `TopicSubscription.subscribe()` has the identical redundant wrapper, and its docstring (46-57) misattributes the savepoint protection to the caller's `try/except` shape — the protection is Django's own `atomic()` inside `get_or_create`, unconditionally. | django-drf | django-drf-reviewer | `backend/packages/wagtail_forum/wagtail_forum/models/subscriptions.py:46-61` | confirmed — same citations as L1; nested `atomic()` is a savepoint. | verified | Agent rated **Medium**; recorded as **Low**. Fix: drop the wrapper and correct the docstring. FIXED — same; docstring now attributes the savepoint to get_or_create itself. |
| L3  | Four side-effect-only tasks omit `ignore_result=True` (every call site is a bare `.delay()`), unlike `sync_blog_page_chunks` at line 372; with `CELERY_RESULT_BACKEND` set, each run would persist a result nobody reads. | celery | celery-async-reviewer | `backend/apps/forum_host/tasks.py:110,197,279,440` | confirmed — Celery docs: default `result_expires` 1 day; per-task `ignore_result` is the idiom (`backend/docs/patterns/domain/celery.md`). | verified | Verified. Moot until H1 (nothing executes), then a one-liner per task. FIXED — `ignore_result=True` on all four; pinned by `test_side_effect_only_tasks_ignore_results` over all five forum tasks. |
| L4  | Composer drafts are keyed `forum-draft:<kind>:<id>` in `sessionStorage` with no user scope, and `logout()` only removes the `user` key, so a same-tab logout → login-as-another-user surfaces user A's unsent draft to user B. | react-typescript | react-typescript-reviewer | `web/src/utils/forumDrafts.ts:7-9`, `web/src/services/authService.ts:236-241` | confirmed — MDN: `sessionStorage` lives for the tab, no auto-clear on auth change. | verified | Agent rated **Medium**; recorded as **Low**: per-tab storage, shared-device same-tab scenario, drafts are for public posts. Fix: clear `forum-draft:*` on logout or key by user id. FIXED — `clearAllDrafts()` (collect-then-remove over `forum-draft:*`) called from `logout()` on both paths; tests in `forumDrafts.test.ts` and `authService.test.ts`. Phase 6 (cross-cutting, MEDIUM, fixed): a swap can also arrive passively (focus revalidation finds another cookie; expired session then a different login), so `AuthContext` now clears on a change between two real accounts — never on mount/reload or an expire → same-account re-login; 2 tests. |
| L5  | TipTap mention `suggestion.render()` (dropdown create/position/keyboard/cleanup, ~115 lines) has no coverage at either tier: the unit test uses a headless `Editor` (never fires `onStart`/`onUpdate`), and no Playwright spec types `@`. | react-typescript | react-typescript-reviewer | `web/src/components/forum/forumMentionNode.ts:77-193`, `forumMentionNode.test.ts`, `web/e2e/*` | confirmed — TipTap v3 Suggestion docs: `render()` hooks need a mounted `EditorView` (`mount`, `clientRect`); no documented headless test path. | verified | Agent rated **Medium**; recorded as **Low**: coverage gap, no observed defect. Fix: one Playwright spec. FIXED — 5 tests drive `render()`'s onStart/onUpdate/onKeyDown/onExit directly with plugin-shaped props: mount + position, wrap-around arrow navigation, Enter and mousedown commit, teardown, empty-list / destroyed-editor guards. Phase 6 (react reviewer): the plugin-contract half still wants the Playwright spec `testing.md` prescribes → `todos/336-pending-p4-forum-mention-dropdown-e2e.md`. |
| L6  | Icon-only (emoji) reaction button has `min-h-11` but not `min-w-11`; its sibling "Add reaction" button (line 395) was fixed with both in PR #623. Only icon-only instance left in `web/src`. | react-typescript | react-typescript-reviewer | `web/src/components/forum/PostCard.tsx:413` (vs `:395`) | confirmed — WCAG 2.2 SC 2.5.8: padding counts, so it clears the 24 px AA floor; it breaks the project's own 44 px convention (`forumMentionNode.ts:104`). | verified | Verified. One-class fix. FIXED — `min-w-11 justify-center` added, matching the sibling. |
| L7  | `<p role="status" aria-live="polite">` co-mounted with `<LoadingSpinner />`, which itself renders `role="status"`; the explicit `aria-live` is redundant (implicit for `status`). | react-typescript | react-typescript-reviewer | `web/src/components/forum/EditHistoryDialog.tsx:143,148`, `web/src/components/ui/LoadingSpinner.tsx:21` | confirmed — MDN `status` role: implicit `aria-live="polite"`; multiple status regions are permitted. Test-query ergonomics (`getByRole('status')`), not an a11y defect. | verified | Verified. Fix: bare `aria-live`, per `ThreadDetailPage`'s notice. FIXED — `role="status"` dropped (bare `aria-live="polite"`); the test now queries `p[aria-live="polite"]` inside the dialog and asserts no role. |
| L8  | Composer has no `PopScope` back-navigation confirmation while title/body hold unsent text. Product decision, not a defect. | flutter | flutter-dart-reviewer | `plant_community_mobile/lib/features/forum/screens/forum_composer_screen.dart` | confirmed (optional) — Flutter docs: `PopScope` + `onPopInvokedWithResult` is the current API; composes with go_router. | verified | Verified no `PopScope`/`WillPopScope` in file. FIXED — `PopScope(canPop: !_hasUnsentInput)` + discard dialog; the title controller now also triggers the rebuild listener. 5 widget tests behind a real route (ask + keep, discard, empty leaves, published submit pops with no prompt, moderation-queued submit pops with no prompt, title-only in topic mode, image-only). Phase 6 (flutter reviewer, HIGH, fixed): `canPop` also had to honour `_pending` — the queued text stays in the controllers behind the pending view but has been sent; the title field's redundant `onChanged` was dropped too. |
| L9  | `ForumSyncService.sync()` persists only after the full `has_more` walk (so a page-2 failure must leave the store untouched), but no test drives a mid-walk failure or the `_maxPages` (500) bound. | flutter | flutter-dart-reviewer | `plant_community_mobile/lib/features/forum/services/forum_sync_service.dart:27-54`, `test/features/forum/services/forum_sync_service_test.dart` | — | verified | Verified: multi-page success test exists (line 99), no failure case. FIXED — 2 tests: a page-2 failure leaves the persisted mirror and cursor untouched; a has_more-forever contract stops at `maxPages` (500 calls). `_maxPages` made public for the pin; Phase 6: the test pins the value 500 itself, not just the constant (todo 321 lesson). |
| L10 | `Post` is `Indexed` with `SearchField("body")` but no `AutocompleteField`, while `PostViewSet.search_fields = ["body"]` enables the admin search box — so admin post search matches whole words only. Surfaces as a `RuntimeWarning` in the baseline test run. `Topic` already has `AutocompleteField("title")` (todo 276 / audit L8). | wagtail | auditor (baseline warning) | `backend/packages/wagtail_forum/wagtail_forum/models/posts.py:77-96`, `wagtail_hooks.py:85-96` (vs `topics.py:108`) | confirmed — Wagtail search docs: `AutocompleteField` for partial-word admin search; backfill with `manage.py update_index`. | verified | Verified in code. FIXED — `index.AutocompleteField("body")`; `test_post_listing_search_matches_a_body_prefix`. **Prod follow-up:** run `manage.py update_index` once to backfill existing posts (new saves index automatically). |
| L11 | `@mention` autocomplete runs `username__istartswith` and the custom `User.username` is `unique=True` with no `db_index=True`, so Django never created the pattern-ops index and the plain unique B-tree cannot serve a `LIKE` prefix under a non-C locale; the query is a sequential scan that scales with the user table. A GIN trigram index (the project's `icontains` fix) is the wrong shape here. | performance | cross-cutting-reviewer | `backend/packages/wagtail_forum/wagtail_forum/api/user_search.py:63`, `backend/apps/users/migrations/0001_initial.py:54-66` | confirmed — Django PostgreSQL notes (pattern-ops index only on `db_index=True`), PostgreSQL §11.10 opclasses, locale docs. Correct declaration: `Index(OpClass(Upper("username"), name="varchar_pattern_ops"), name=...)` in `apps/users` (outside the forum tree). | verified | Agent rated **Medium**; recorded as **Low**: at today's user count the planner would seq-scan regardless; scaling-latent, not an active hot path. Verify with `EXPLAIN` + `pg_indexes.indexdef` per todo 323. FIXED — `users/0011_username_upper_pattern_index.py` (vendor-guarded RunPython, literal SQL, reversible); `apps/users/tests/test_indexes.py` pins the indexdef AND proves the planner serves the real ORM query from it with `enable_seqscan = off`. Correction to the research note: the ORM casts to `text`, so `text_pattern_ops` is the matching opclass, not `varchar_pattern_ops`. Phase 6 (django reviewer, MEDIUM, fixed): built `CONCURRENTLY` with `atomic = False` like blog 0012/0014 — a plain build takes an ACCESS EXCLUSIVE lock on `auth_user`. |
| L12 | `ExpertsView` and `MyBlocksView` both `select_related` through an avatar leg, but no fixture in their tests sets an avatar and `MyBlocksView` has no query-count pin at all; `test_experts` filters captured SQL by the profile-table substring, so deleting `"avatar"` from either chain would ship green (query-optimization Pattern 30; `test_notifications_api.py:368-407` shows the house style). | testing | cross-cutting-reviewer | `backend/packages/wagtail_forum/wagtail_forum/api/views.py:2021-2024`, `api/user_blocks.py:93-96`, `tests/api/test_experts.py:124-137`, `tests/api/test_user_blocks_api.py` | — | verified | Agent rated **Medium**; recorded as **Low**: no live N+1 today (both chains are intact), purely regression-detection. Verified: zero `avatar` mentions in `test_experts.py`, zero query pins in `test_user_blocks_api.py`. FIXED — avatar fixtures + exact pins: experts = 1 query, my-blocks = 2 (presence touch + the joined SELECT). Mutation-checked: dropping the avatar leg / the select_related fails each. Phase 6 (django reviewer, MEDIUM, fixed): the my-blocks pin now `cache.delete`s the presence-throttle key first — the test cache is Redis when reachable and outlives DB rebuilds, so a recycled pk could skip the UPDATE. |

### Clean (verified, not just asserted)

- **Backend security / API design:** zero new findings from a route-by-route permission/throttle/ownership matrix over both `api_urls.py` files — IDOR on every mutating endpoint (polls, bookmarks, subscriptions, solutions, posts, DMs, blocks, notifications), `_visible_boards()` / `_exclude_blocked_authors()` primitives, no `get_permissions()` overrides in scope, throttle scopes mechanically diffed against `DEFAULT_FORUM_RATELIMITS` (0 mismatches), nh3 sanitizer allowlist, 4-layer upload validation with bounded renditions, AI budget peek-then-consume with hard timeouts, idempotency keys scoped `scope:user` + fingerprinted, no user content logged verbatim, `OPENAI_API_KEY` read lazily.
- **Django/DRF:** every other `except IntegrityError` site (`polls.py:78`, `reports.py:160,227`, `views.py:746,1503`) makes a product decision from the caught constraint rather than retrying a `.get()`; revision-history and public-profile IDOR gates, `MeProfileSerializer` mass-assignment, cursor-ordering pins, redirect fan-out atomicity all correct.
- **Wagtail:** `isinstance()` used consistently for page-type checks; settings cache-token rotation deferred to `on_commit`; `list_export`/inspect fields avoid nullable-FK dotting; admin URLs `reverse()`d; `SummaryItem` matches Wagtail 7.4 `Component`; page seeding resolves `Site.root_page` and publishes; RSS/sitemap scoped to `.live().public()`.
- **Performance:** board-slug rename redirect maintenance is genuinely bulk (three fixed statements, pinned at 1000 topics); counters use single-UPDATE/`F()` throughout; view/last-seen touches cache-throttled and `on_commit`-deferred; 12 list endpoints carry query-count pins; no unbounded querysets, no user-scope cache-key violations, zero `skip`/`xfail`/hollow tests across ~104 test files.
- **Celery:** `send_forum_push` permanent-vs-transient FCM classification (M33) and pinned exponential backoff; blog page-lifecycle receivers use `on_commit` with the `try` inside the callback; `sync_blog_page_chunks` idempotent swap; `moderation_decided` dispatch is correctly outside the creation transaction; LLM spam thread-pool timeout/billing/fail-closed matches PRs #619/#620; prune cron idempotent and non-overlapping.
- **Web:** no `dangerouslySetInnerHTML` on forum content; `forumBody.ts` escapes text nodes and never builds HTML from strings; TipTap link allowlist + `rel="noopener noreferrer"`; `SANITIZE_PRESETS.FORUM` mirrors the backend nh3 list; all router imports from `react-router-dom`; debounce timers via `useRef`; monotonic generation refs on every paginated fetch; object-URL lifecycle in the image alt-prompt revoked on every exit path.
- **Mobile:** zero `StreamSubscription`/`Timer`/`.listen(` in the feature (no dispose gaps possible); `.g.dart` in sync with source (same commit, 9/9 providers, all autoDispose); idempotency-key rotate-on-change/reuse-on-retry tested; composing-range reset on every `copyWith`; link hrefs allowlisted and only ever shown in a SnackBar (no `url_launcher`); JSON parsing uses `as T?` + fallbacks, unknown enum/block values degrade instead of throwing; 48 px reaction targets.

## Deferred Items

Items marked `deferred` must have a linked todo and rationale.

| ID  | Todo | Rationale |
| --- | ---- | --------- |
| H1  | [335-pending-p2-forum-celery-worker.md](../../todos/335-pending-p2-forum-celery-worker.md) | The fix is provisioning a Celery worker (or gating the enqueue sites), i.e. a deploy-topology and monthly-spend decision the user asked to file rather than make in an automated run. Impact is bounded today (bell notifications are synchronous, no client calls the summary endpoint, Redis backlog not measurable); promote to p1 the day push or email is announced. |

## Summary

| Severity  | Found | Verified | Deferred | False-positive | Open   |
| --------- | ----- | -------- | -------- | -------------- | ------ |
| Critical  | 0     | 0        | 0        | 0              | 0     |
| High      | 1     | 0        | 1        | 0              | 0     |
| Medium    | 8     | 8        | 0        | 0              | 0     |
| Low       | 12    | 12       | 0        | 0              | 0     |
| **Total** | 21    | 20       | 1        | 0              | **0** |

## Fix Commits

| Commit | Description |
| ------ | ----------- |
| _(filled at commit)_ | `fix: resolve forum audit findings (20 verified, 1 deferred)` on `audit/forum-2026-09-04` |

## Codification (Phase 8)

| Finding | Destination | Note |
| ------- | ----------- | ---- |
| —       | —           | — (read-only run; candidates once fixes land: M1 → `docs/rules/wagtail.md` "draft-state snippets write through to the row — snapshot from `live_revision`, not the DB row"; L1/L2 → `docs/rules/triggers.json` trigger for `except IntegrityError:` immediately after `get_or_create(`; M8/L3 → `docs/rules/celery.md` already covers both) |
